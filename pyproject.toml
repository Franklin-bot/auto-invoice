[project]
name = "auto-invoice"
version = "0.1.0"
description = "Generate invoice PDFs from a LaTeX template."
requires-python = ">=3.11"
dependencies = [
    "reportlab>=4.4.0",
]

[tool.uv]
package = false
